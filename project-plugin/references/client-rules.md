# Regras do cliente — Calculadora de Preço por Ponto

- Regras de preço (R1–R4) só mudam com aprovação do Comercial do cliente — regra inegociável (PROJECT.md).
- Propostas abaixo da margem mínima exigem aprovação do Comercial — fora do escopo da ferramenta (out of scope).
- Dados operacionais do cliente não saem do SharePoint/repo privado; PII (contatos) nunca entra no Git (DATA_CATALOG).
- Toda mudança que altera preço exibido = tier 2 → /change-number, sem exceção.
